@model EditarPerfilViewModel

<style>
    .form-container {
        max-width: 600px;
        margin: 2rem auto;
        padding: 2rem;
        background-color: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(107, 121, 173, 0.3);
        border-radius: 12px;
        box-shadow: 0 2px 10px rgba(107, 121, 173, 0.2);
        color: #d6d9ef;
    }

    .form-title {
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 2rem;
        text-align: center;
    }

    .form-group label {
        font-weight: 600;
        color: #aab0d0;
    }

    .form-control {
        background-color: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(107, 121, 173, 0.4);
        color: #ffffff;
    }

        .form-control:focus {
            border-color: #6b79ad;
            background-color: rgba(255, 255, 255, 0.15);
            color: #ffffff;
            box-shadow: 0 0 0 0.2rem rgba(107, 121, 173, 0.25);
        }

    .text-danger {
        color: #ff6b6b;
    }

    .btn-primary, .btn-secondary {
        width: 100%;
        padding: 10px;
        font-weight: 600;
        border-radius: 8px;
        border: none;
    }

    .btn-primary {
        background-color: #6b79ad;
        color: #ffffff;
    }

        .btn-primary:hover {
            background-color: #5a6896;
        }

    .btn-secondary {
        background-color: transparent;
        border: 1px solid #6b79ad;
        color: #ffffff;
        margin-top: 10px;
    }

        .btn-secondary:hover {
            background-color: rgba(107, 121, 173, 0.2);
            color: #ffffff;
        }

    .alert-success,
    .alert-danger {
        border-radius: 8px;
        font-weight: 500;
    }
</style>

<div class="form-container">
    <h2 class="form-title">Editar Perfil</h2>

    @if (TempData["EXITO"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["EXITO"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["ERROR"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ERROR"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <form asp-action="EditarPerfil" method="post">
        <div class="form-group mb-3">
            <label asp-for="UserName"></label>
            <input asp-for="UserName" class="form-control" />
            <span asp-validation-for="UserName" class="text-danger"></span>
        </div>

        <div class="form-group mb-3">
            <label asp-for="FirstName"></label>
            <input asp-for="FirstName" class="form-control" />
            <span asp-validation-for="FirstName" class="text-danger"></span>
        </div>

        <div class="form-group mb-4">
            <label asp-for="SecondName"></label>
            <input asp-for="SecondName" class="form-control" />
            <span asp-validation-for="SecondName" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary">Guardar cambios</button>
    </form>

    <a asp-action="ModificarPassword" asp-controller="Usuarios" class="btn btn-secondary">
        Cambiar Contraseña
    </a>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
